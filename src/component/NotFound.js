import React, { Component } from 'react';

// Stateless Component

const NotFound = (props) => (
    <header className="top">
        <h1>NotFound ...!</h1>
    </header>
);

export default NotFound;
